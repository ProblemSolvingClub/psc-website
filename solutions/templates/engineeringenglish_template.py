# We don't know how many inputs we will recieve
try:
    # Store the words we've already seen in this set
    duplicates = set()
    while(True):
        words = input().split()
        # Loop through each word and do something
        # Watch for newlines
        
# Done taking inputs, exit normally
except EOFerror as e:
    raise SystemExit
